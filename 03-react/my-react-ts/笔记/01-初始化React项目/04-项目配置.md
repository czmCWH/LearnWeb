# 一、配置别名路径 @
别名路径`@`的配置分为2步：
- 1、路径解析配置，让打包工具把 `@/` 解析为 `src/`；
- 2、路径联想配置，在 VsCode 中输入 `@/` 时，能自动联想出对应 `src/`下的子级目录；

 
webpack 需借助 `craco` 插件来配置。
vite 中配置时，如果基于 typescript，则需要安装 node 类型包：`npm install @types/node -D`


# 二、重置浏览器默认样式

```shell
$ npm install normalize.css
```